struct Coords {
    x: int,
}

export component AppWindow inherits Window {
    function trigger() -> Coords {
        if false {
            return { };
        }
        { x: 42 }
        // The following works just fine:
        // return { x: 42 };
    }
}
